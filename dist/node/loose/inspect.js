if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}

define([
], function (
) {

    'use strict';


    function inspect(target) {
        return target;
    }

    return inspect;
});